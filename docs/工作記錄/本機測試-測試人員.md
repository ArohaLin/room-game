# 工作記錄 — 測試人員(本機測試)

## 提出的方案
- 三層測試:①靜態語法檢查(node --check)②全流程狀態機自動試玩(模擬玩家照關鍵路徑、驗證每關前置門檻與解鎖鏈)③各謎題真實 UI 答案判定(正確會過、錯誤要擋)+ 視覺驗收。
- 以瀏覽器預覽工具實際載入、截圖、點擊、讀 console。

## 做出的決策
- 對每道謎題同時測「正確通過」與「錯誤被擋」,避免誤判通關。
- 對有前置/缺卡的謎題額外測門檻(P8 缺卡、P9 無 P7)。

## 完成的任務
- ✅ 靜態:data.js / art.js / game.js 三檔 node --check 全通過;console 無 error。
- ✅ 全流程:P1→P1b→P2→P3→GREEN→P4→YELLOW→P5→P6→P7→P8→P9→P10 共 13 關依序全過,物品(鑰匙×2、四色卡)、偵探筆記(8 筆)、結局演出皆正確觸發。
- ✅ UI 答案判定:P2/P3/P7/P8/P9/P10 正確答案皆通過;P2(選錯)、P7(輸 333)、P8(錯序)、P10(錯序)皆正確被擋。
- ✅ 門檻:P8 未集滿四色卡被擋、P9 未解 P7 被擋。
- ✅ 視覺:標題、前院場景、P8 顏色順序謎題、偵探筆記排版皆正確,風格符合溫馨手繪。

## 與其他角色的溝通(回報並修正的 bug)
- 🐞→✅ 開門(P1b)的 `need` 被引擎當「謎題 id」誤判,導致拿了鑰匙仍開不了門 → 改用 `needItem`,程式同步修正。
- 🐞→✅ P2 直接解鎖主臥衛浴,讓玩家可跳過 P5 → 改由 P5 解鎖主臥衛浴,維持依賴正確。
- 🐞→✅ P4 玩具「開局即為答案順序」(等於免解) → 改為開局打亂。
- 結論回報總監:凍結版設計在實機上**公平、單一可通關、無 soft-lock、無 console 錯誤**,可交付驗收。
